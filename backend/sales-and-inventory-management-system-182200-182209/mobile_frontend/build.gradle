buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.4.2")
    }
}
allprojects {
    repositories {
        mavenCentral()
        google()
    }
}
tasks.register("noop") {
    doLast { println("No-op task for CI") }
}
defaultTasks("noop")

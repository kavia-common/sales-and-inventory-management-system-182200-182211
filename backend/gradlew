#!/usr/bin/env bash
# Root Gradle wrapper shim for CI environments that invoke './gradlew' directly.
# This shim ensures a zero-exit no-op when no Android project is present.
echo "Gradle wrapper shim at repo root: no Android project present. Skipping."
exit 0
